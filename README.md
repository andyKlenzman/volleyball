# volleyball

Serve tracker for volleyball practice. Keeps a roster of players, logs serves per session, and lets you configure team colors.

Built with React and Vite.

```bash
npm install
npm run dev
```
